/**
 * @file form-components-optimized.css
 * @description Optimized shared styles for form input components and role cards
 * @author Sherif Talaat
 * @version 2.1.0
 * @date 15-10-2025
 * @last-updated 15-10-2025 Optimized for performance and maintainability
 */
@import './globals.css';

/* === Shared Form Component Styles === */
.form-input__wrapper,
.form-textarea__wrapper,
.form-select__wrapper {
    position: relative;
    width: 100%;
    border-radius: 12px;
    background: var(--primary-black);
    border: 2px solid var(--translucent-black-1);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 20px var(--shadow-pink);
    background-image: radial-gradient(circle at 10% 20%, var(--radial-pink-1) 0%, transparent 20%), radial-gradient(circle at 90% 80%, var(--radial-pink-2) 0%, transparent 20%);
    animation: slideIn 0.3s ease-out;
}

    .form-input__wrapper:hover,
    .form-textarea__wrapper:hover,
    .form-select__wrapper:hover {
        border-color: var(--accent-pink);
        box-shadow: 0 6px 25px var(--shadow-pink);
        transform: translateY(-2px);
    }

    .form-input__wrapper:focus-within,
    .form-textarea__wrapper:focus-within,
    .form-select__wrapper:focus-within {
        border-color: var(--vivid-pink);
        box-shadow: 0 0 0 3px var(--radial-pink-1), 0 8px 30px var(--shadow-pink);
        transform: translateY(-2px);
        animation: glowPulse 2s infinite;
    }

.form-input__container,
.form-textarea__container,
.form-select__container {
    width: 100%;
    margin-bottom: 1.5rem;
}

/* Icon Styles */
.form-input__icon,
.form-textarea__icon,
.form-select__icon {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    left: 16px;
    color: var(--muted-foreground);
    transition: color 0.3s ease;
    z-index: 1;
}

    .form-select__icon.right {
        right: 16px;
        left: auto;
        pointer-events: none;
    }

.form-input__wrapper:hover .form-input__icon,
.form-textarea__wrapper:hover .form-textarea__icon,
.form-select__wrapper:hover .form-select__icon,
.form-input__wrapper:focus-within .form-input__icon,
.form-textarea__wrapper:focus-within .form-textarea__icon,
.form-select__wrapper:focus-within .form-select__icon {
    color: var(--accent-pink);
}

.form-textarea__icon {
    top: 20px;
    transform: none;
}

/* Field Styles */
.form-input__field,
.form-textarea__field,
.form-select__field {
    width: 100%;
    padding: 16px 52px 16px 48px;
    font-size: 16px;
    font-weight: 500;
    line-height: 1.5;
    color: var(--primary-white);
    background: transparent;
    border: none;
    border-radius: 12px;
    outline: none;
    transition: background 0.3s ease;
    font-family: var(--font-sans);
}

    .form-input__field:focus,
    .form-textarea__field:focus,
    .form-select__field:focus {
        background: var(--translucent-white-3);
    }

    .form-input__field::placeholder,
    .form-textarea__field::placeholder {
        color: var(--muted-foreground);
        font-weight: 400;
    }

/* Textarea Specific */
.form-textarea__field {
    min-height: 120px;
    padding: 20px 52px;
    line-height: 1.6;
    resize: vertical;
}

/* Select Specific */
.form-select__field {
    appearance: none;
    cursor: pointer;
}

    .form-select__field:invalid {
        color: var(--muted-foreground);
    }

    .form-select__field option {
        padding: 12px 16px;
        font-size: 15px;
        font-weight: 500;
        color: var(--primary-white);
        background: var(--primary-black);
        border-bottom: 1px solid var(--border);
    }

        .form-select__field option:checked {
            background: linear-gradient(135deg, var(--accent-pink), var(--vivid-pink));
            color: var(--primary-black);
            font-weight: 600;
        }

        .form-select__field option:hover {
            background: var(--light-pink);
            color: var(--primary-white);
        }

    .form-select__field:valid {
        color: var(--primary-white);
    }

/* Password Toggle */
.form-input__password-toggle {
    position: absolute;
    top: 50%;
    right: 16px;
    transform: translateY(-50%);
    background: none;
    border: none;
    color: var(--muted-foreground);
    cursor: pointer;
    padding: 8px;
    border-radius: 6px;
    transition: all 0.3s ease;
    z-index: 1;
}

    .form-input__password-toggle:hover {
        color: var(--accent-pink);
        background: var(--translucent-white-1);
    }

    .form-input__password-toggle:active {
        transform: translateY(-50%) scale(0.95);
    }

.form-input__wrapper:has(.form-input__password-toggle) .form-input__field {
    padding-right: 84px;
}

/* State Modifiers */
.form-input__wrapper--error,
.form-textarea__wrapper--error,
.form-select__wrapper--error {
    border-color: var(--destructive);
}

    .form-input__wrapper--error:focus-within,
    .form-textarea__wrapper--error:focus-within,
    .form-select__wrapper--error:focus-within {
        border-color: var(--destructive);
        box-shadow: 0 0 0 3px var(--radial-pink-1), 0 8px 30px var(--shadow-pink);
    }

    .form-input__wrapper--error .form-input__icon,
    .form-textarea__wrapper--error .form-textarea__icon,
    .form-select__wrapper--error .form-select__icon {
        color: var(--destructive);
    }

.form-input__wrapper--success,
.form-textarea__wrapper--success,
.form-select__wrapper--success {
    border-color: var(--accent-pink);
}

    .form-input__wrapper--success:focus-within,
    .form-textarea__wrapper--success:focus-within,
    .form-select__wrapper--success:focus-within {
        border-color: var(--vivid-pink);
        box-shadow: 0 0 0 3px var(--radial-pink-1), 0 8px 30px var(--shadow-pink);
    }

    .form-input__wrapper--success .form-input__icon,
    .form-textarea__wrapper--success .form-textarea__icon,
    .form-select__wrapper--success .form-select__icon {
        color: var(--accent-pink);
    }

.form-input__wrapper:disabled,
.form-textarea__wrapper:disabled,
.form-select__wrapper:disabled {
    background: var(--muted);
    border-color: var(--border);
    box-shadow: none;
}

.form-input__field:disabled,
.form-textarea__field:disabled,
.form-select__field:disabled {
    background: var(--muted);
    color: var(--muted-foreground);
    cursor: not-allowed;
}

.form-input__wrapper:disabled .form-input__icon,
.form-textarea__wrapper:disabled .form-textarea__icon,
.form-select__wrapper:disabled .form-select__icon {
    color: var(--muted-foreground);
}

/* Size Variants */
.form-input__wrapper--sm .form-input__field,
.form-textarea__wrapper--sm .form-textarea__field,
.form-select__wrapper--sm .form-select__field {
    padding: 12px 44px 12px 40px;
    font-size: 14px;
}

.form-textarea__wrapper--sm .form-textarea__field {
    min-height: 100px;
    padding: 16px 44px;
}

.form-input__wrapper--lg .form-input__field,
.form-textarea__wrapper--lg .form-textarea__field,
.form-select__wrapper--lg .form-select__field {
    padding: 20px 56px 20px 52px;
    font-size: 18px;
}

.form-textarea__wrapper--lg .form-textarea__field {
    min-height: 160px;
    padding: 24px 56px;
}

/* === Role Card Styles === */
.role-card {
    position: relative;
    width: 100%;
    padding: 2rem;
    border-radius: 16px;
    background: var(--primary-black);
    border: 2px solid var(--translucent-black-1);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 4px 20px var(--shadow-pink);
    background-image: radial-gradient(circle at 0% 0%, var(--radial-pink-1) 0%, transparent 50%), radial-gradient(circle at 100% 100%, var(--radial-pink-2) 0%, transparent 50%);
    cursor: pointer;
    outline: none;
    text-align: left;
    animation: cardSlideIn 0.4s ease-out;
}

    .role-card:hover {
        border-color: var(--accent-pink);
        box-shadow: 0 8px 30px var(--shadow-pink);
        transform: translateY(-4px);
    }

    .role-card:focus {
        border-color: var(--vivid-pink);
        box-shadow: 0 0 0 3px var(--radial-pink-1), 0 8px 30px var(--shadow-pink);
        transform: translateY(-2px);
    }

    .role-card:active {
        transform: translateY(-1px);
        transition: all 0.1s ease;
    }

    .role-card[aria-pressed="true"] {
        border-color: var(--vivid-pink);
        background: linear-gradient(135deg, var(--primary-black) 0%, var(--radial-pink-3) 100%);
        box-shadow: 0 0 0 2px var(--vivid-pink), 0 8px 30px var(--shadow-pink);
        transform: translateY(-2px);
        animation: glowPulse 2s infinite;
    }

        .role-card[aria-pressed="true"]::after {
            content: "✓";
            position: absolute;
            top: 12px;
            right: 12px;
            width: 24px;
            height: 24px;
            background: var(--vivid-pink);
            color: var(--primary-black);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.875rem;
            font-weight: 600;
        }

.role-card__icon {
    width: 3.5rem;
    height: 3.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--translucent-white-1);
    border-radius: 12px;
    color: var(--accent-pink);
    font-size: 1.5rem;
    margin-bottom: 1rem;
    transition: all 0.3s ease;
    border: 1px solid var(--translucent-black-1);
}

.role-card:hover .role-card__icon {
    background: var(--radial-pink-1);
    color: var(--vivid-pink);
    transform: scale(1.05);
}

.role-card[aria-pressed="true"] .role-card__icon {
    background: linear-gradient(135deg, var(--accent-pink), var(--vivid-pink));
    color: var(--primary-black);
    transform: scale(1.05);
}

.role-card__title {
    font-size: 1.25rem;
    font-weight: 600;
    line-height: 1.3;
    color: var(--primary-white);
    margin-bottom: 0.75rem;
    transition: color 0.3s ease;
}

.role-card:hover .role-card__title,
.role-card[aria-pressed="true"] .role-card__title {
    color: var(--accent-pink);
}

.role-card__description {
    font-size: 0.875rem;
    line-height: 1.5;
    color: var(--muted-foreground);
    margin: 0;
    transition: color 0.3s ease;
}

.role-card:hover .role-card__description {
    color: var(--primary-white);
}

.role-cards-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
}

/* === Theme Toggle === */
.theme-toggle__button--compact {
    min-width: auto;
    width: 2.5rem;
    height: 2.5rem;
    padding: 0.5rem;
    border-radius: 50%;
}

    .theme-toggle__button--compact .theme-toggle__text {
        display: none;
    }

    .theme-toggle__button--compact .theme-toggle__icon {
        margin: 0;
    }

/* === Responsive Design === */
@media (max-width: 75rem) {
    .form-input__field,
    .form-textarea__field,
    .form-select__field {
        padding: 0.875rem 3rem;
        font-size: 0.875rem;
        border-radius: 0.75rem;
    }

    .form-textarea__field {
        padding: 1rem 3rem;
        min-height: 110px;
    }

    .form-input__icon,
    .form-textarea__icon,
    .form-select__icon {
        font-size: 1rem;
    }

        .form-input__icon.left,
        .form-textarea__icon.left,
        .form-select__icon.left {
            left: 1.125rem;
        }

        .form-select__icon.right {
            right: 1.125rem;
        }

    .form-textarea__icon {
        top: 1.125rem;
    }

    .form-input__password-toggle {
        right: 1.125rem;
        padding: 6px;
    }

    .role-card {
        padding: 1.75rem;
        border-radius: 14px;
    }

    .role-card__icon {
        width: 3rem;
        height: 3rem;
        font-size: 1.375rem;
        margin-bottom: 0.875rem;
    }

    .role-card__title {
        font-size: 1.125rem;
        margin-bottom: 0.625rem;
    }

    .role-card__description {
        font-size: 0.8125rem;
    }

    .role-cards-grid {
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 1.25rem;
    }
}

@media (max-width: 60.5rem) {
    .form-input__field,
    .form-textarea__field,
    .form-select__field {
        padding: 0.75rem 2.5rem;
        font-size: 0.875rem;
        border-radius: 0.75rem;
    }

    .form-textarea__field {
        padding: 0.875rem 2.5rem;
        min-height: 100px;
    }

    .form-input__icon,
    .form-textarea__icon,
    .form-select__icon {
        font-size: 1rem;
    }

        .form-input__icon.left,
        .form-textarea__icon.left,
        .form-select__icon.left {
            left: 1rem;
        }

        .form-select__icon.right {
            right: 1rem;
        }

    .form-textarea__icon {
        top: 1rem;
    }

    .form-input__password-toggle {
        right: 1rem;
    }

    .role-card {
        padding: 1.5rem;
        border-radius: 12px;
    }

    .role-card__icon {
        width: 2.75rem;
        height: 2.75rem;
        font-size: 1.25rem;
        margin-bottom: 0.75rem;
    }

    .role-card__title {
        font-size: 1.0625rem;
        margin-bottom: 0.5rem;
    }

    .role-card__description {
        font-size: 0.8125rem;
        line-height: 1.4;
    }

    .role-card:hover {
        transform: translateY(-2px);
    }

    .role-cards-grid {
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: 1rem;
    }
}

@media (max-width: 40rem) {
    .form-input__field,
    .form-textarea__field,
    .form-select__field {
        padding: 0.625rem 2rem;
        font-size: 0.875rem;
        border-radius: 0.625rem;
    }

    .form-textarea__field {
        min-height: 100px;
    }

    .form-input__icon,
    .form-textarea__icon,
    .form-select__icon {
        font-size: 0.875rem;
    }

        .form-input__icon.left,
        .form-textarea__icon.left,
        .form-select__icon.left {
            left: 0.75rem;
        }

        .form-select__icon.right {
            right: 0.75rem;
        }

    .form-textarea__icon {
        top: 0.75rem;
    }

    .form-input__password-toggle {
        right: 0.75rem;
        padding: 4px;
    }

    .form-input__wrapper,
    .form-textarea__wrapper,
    .form-select__wrapper {
        border-width: 1.5px;
        box-shadow: 0 2px 10px var(--shadow-pink);
    }

        .form-input__wrapper:hover,
        .form-textarea__wrapper:hover,
        .form-select__wrapper:hover,
        .form-input__wrapper:focus-within,
        .form-textarea__wrapper:focus-within,
        .form-select__wrapper:focus-within {
            transform: translateY(-1px);
            box-shadow: 0 3px 15px var(--shadow-pink);
            animation: none;
        }

        .form-input__wrapper:focus-within,
        .form-textarea__wrapper:focus-within,
        .form-select__wrapper:focus-within {
            box-shadow: 0 0 0 2px var(--radial-pink-1), 0 4px 20px var(--shadow-pink);
        }

    .role-card {
        padding: 1.25rem;
        border-radius: 10px;
        border-width: 1.5px;
    }

    .role-card__icon {
        width: 2.5rem;
        height: 2.5rem;
        font-size: 1.25rem;
        margin-bottom: 0.75rem;
        border-radius: 8px;
    }

    .role-card__title {
        font-size: 1rem;
        margin-bottom: 0.5rem;
    }

    .role-card__description {
        font-size: 0.8125rem;
        line-height: 1.4;
    }

    .role-card:hover,
    .role-card:focus {
        transform: translateY(-1px);
        box-shadow: 0 6px 20px var(--shadow-pink);
    }

    .role-card:focus {
        box-shadow: 0 0 0 2px var(--radial-pink-1), 0 4px 20px var(--shadow-pink);
    }

    .role-card[aria-pressed="true"]::after {
        top: 8px;
        right: 8px;
        width: 20px;
        height: 20px;
        font-size: 0.75rem;
    }

    .role-cards-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
    }

    .form-input__wrapper:focus-within,
    .form-textarea__wrapper:focus-within,
    .form-select__wrapper:focus-within,
    .role-card[aria-pressed="true"] {
        animation: none;
    }
}

/* === Utility Styles === */
.form-textarea__field::-webkit-scrollbar,
.form-select__field::-webkit-scrollbar {
    width: 8px;
}

.form-textarea__field::-webkit-scrollbar-track,
.form-select__field::-webkit-scrollbar-track {
    background: var(--muted);
    border-radius: 4px;
}

.form-textarea__field::-webkit-scrollbar-thumb,
.form-select__field::-webkit-scrollbar-thumb {
    background: var(--accent-pink);
    border-radius: 4px;
}

    .form-textarea__field::-webkit-scrollbar-thumb:hover,
    .form-select__field::-webkit-scrollbar-thumb:hover {
        background: var(--vivid-pink);
    }

@media (max-width: 40rem) {
    .form-textarea__field::-webkit-scrollbar,
    .form-select__field::-webkit-scrollbar {
        width: 6px;
    }
}

.role-card:focus-visible {
    outline: 2px solid var(--vivid-pink);
    outline-offset: 2px;
}

/* === Reduced Motion === */
@media (prefers-reduced-motion: reduce) {
    .form-input__wrapper,
    .form-textarea__wrapper,
    .form-select__wrapper,
    .role-card {
        animation: none;
        transition: none;
    }

        .form-input__wrapper:hover,
        .form-textarea__wrapper:hover,
        .form-select__wrapper:hover,
        .form-input__wrapper:focus-within,
        .form-textarea__wrapper:focus-within,
        .form-select__wrapper:focus-within,
        .role-card:hover,
        .role-card:focus,
        .role-card[aria-pressed="true"] {
            transform: none;
        }

    .form-input__icon,
    .form-textarea__icon,
    .form-select__icon,
    .role-card__icon {
        transition: none;
    }
}

/* === Animations === */
@keyframes slideIn {
    from {
        opacity: 0;
        transform: translateY(-8px);
    }

    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes glowPulse {
    0%, 100% {
        box-shadow: 0 4px 20px var(--shadow-pink);
    }

    50% {
        box-shadow: 0 6px 25px var(--shadow-pink), 0 0 20px var(--radial-pink-1);
    }
}

@keyframes cardSlideIn {
    from {
        opacity: 0;
        transform: translateY(20px) scale(0.95);
    }

    to {
        opacity: 1;
        transform: translateY(0) scale(1);
    }
}
==================================================================
/**
 * @fileoverview Global styles for Job Magnet application
 * @description Contains CSS custom properties, reset styles, and global theme configuration
 * @author Sherif Talaat
 * @version 1.2.0
 * @date 10-10-2025
 */

/*
    * Changes Made: 
    * - Added CSS reset for better cross-browser consistency
    * - Defined global utility classes for common patterns
    * - Enhanced focus styles for accessibility
    * - Optimized variable naming for clarity
    * - Ensured compatibility with Tailwind CSS
    * - Edited Dark Theme support
    * Date: 14-10-2025 By Sherif Talaat
*/
@import "tailwindcss";
@import "tw-animate-css";

/* Global rules */
* {
    margin: 0;
    box-sizing: border-box;
    transition: background-color 0.5s ease, color 0.5s ease, border-color 0.5s ease;
}
    /* CSS Reset and Base Styles */
    *,
    *::before,
    *::after {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

:root {
    /* Default Light Theme Variables */
    --background: oklch(0.985 0 0);
    --foreground: oklch(0.145 0 0);
    --card: oklch(0.985 0 0);
    --card-foreground: oklch(0.145 0 0);
    --popover: oklch(0.985 0 0);
    --popover-foreground: oklch(0.145 0 0);
    --primary: oklch(0.145 0 0);
    --primary-foreground: oklch(0.985 0 0);
    --secondary: oklch(0.85 0 0);
    --secondary-foreground: oklch(0.145 0 0);
    --muted: oklch(0.85 0 0);
    --muted-foreground: oklch(0.4 0 0);
    --accent: oklch(0.85 0 0);
    --accent-foreground: oklch(0.145 0 0);
    --destructive: oklch(0.95 0.1 25);
    --destructive-foreground: oklch(0.4 0.2 25);
    --border: oklch(0.85 0 0);
    --input: oklch(0.95 0 0);
    --ring: oklch(0.6 0 0);
    --chart-1: oklch(0.488 0.243 264.376);
    --chart-2: oklch(0.696 0.17 162.48);
    --chart-3: oklch(0.769 0.188 70.08);
    --chart-4: oklch(0.627 0.265 303.9);
    --chart-5: oklch(0.645 0.246 16.439);
    --sidebar: oklch(0.95 0 0);
    --sidebar-foreground: oklch(0.145 0 0);
    --sidebar-primary: oklch(0.488 0.243 264.376);
    --sidebar-primary-foreground: oklch(0.145 0 0);
    --sidebar-accent: oklch(0.85 0 0);
    --sidebar-accent-foreground: oklch(0.145 0 0);
    --sidebar-border: oklch(0.85 0 0);
    --sidebar-ring: oklch(0.6 0 0);
    /* Custom Job Magnet Light Theme */
    --primary-black: #ffffff;
    --primary-white: #1a1a1a;
    --accent-pink: #e91e63;
    --vivid-pink: #d81b60;
    --light-pink: #f8bbd0;
    --radial-pink-1: rgba(233, 30, 99, 0.1);
    --radial-pink-2: rgba(233, 30, 99, 0.07);
    --radial-pink-3: rgba(233, 30, 99, 0.05);
    --translucent-white-1: rgba(0, 0, 0, 0.05);
    --translucent-white-2: rgba(0, 0, 0, 0.1);
    --translucent-white-3: rgba(0, 0, 0, 0.02);
    --translucent-white-4: rgba(0, 0, 0, 0.7);
    --translucent-white-5: rgba(0, 0, 0, 0.5);
    --translucent-black-1: rgba(255, 255, 255, 0.7);
    --select-pink: #c2185b;
    --submit-pink-1: #e91e63;
    --submit-pink-2: #d81b60;
    --shadow-pink: #f0629255;
    --transparent: transparent;
}

.dark {
    /* Typography */
    --font-sans: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    --font-mono: "Courier New", monospace;
    /* Custom Job Magnet theme with blue gradients */
    --primary-black: #000000;
    --primary-white: #ffffff;
    --accent-pink: #ff6bab;
    --vivid-pink: #ff0055;
    --light-pink: #f0e2eb;
    --radial-pink-1: rgba(246, 92, 166, 0.174);
    --radial-pink-2: rgba(246, 92, 159, 0.121);
    --radial-pink-3: rgba(236, 72, 148, 0.094);
    --translucent-white-1: rgba(255, 255, 255, 0.1);
    --translucent-white-2: rgba(255, 255, 255, 0.742);
    --translucent-white-3: rgba(255, 255, 255, 0.04);
    --translucent-white-4: rgba(255, 255, 255, 0.9);
    --translucent-white-5: rgba(255, 255, 255, 0.7);
    --translucent-black-1: rgba(0, 0, 0, 0.204);
    --select-pink: #800033;
    --submit-pink-1: #e6005c;
    --submit-pink-2: #ff5197;
    --shadow-pink: #fd94be55;
    --transparent: transparent;
}

@theme inline {
    /* optional: --font-sans, --font-serif, --font-mono if they are applied in the layout.tsx */
    --color-background: var(--background);
    --color-foreground: var(--foreground);
    --color-card: var(--card);
    --color-card-foreground: var(--card-foreground);
    --color-popover: var(--popover);
    --color-popover-foreground: var(--popover-foreground);
    --color-primary: var(--primary);
    --color-primary-foreground: var(--primary-foreground);
    --color-secondary: var(--secondary);
    --color-secondary-foreground: var(--secondary-foreground);
    --color-muted: var(--muted);
    --color-muted-foreground: var(--muted-foreground);
    --color-accent: var(--accent);
    --color-accent-foreground: var(--accent-foreground);
    --color-destructive: var(--destructive);
    --color-destructive-foreground: var(--destructive-foreground);
    --color-border: var(--border);
    --color-input: var(--input);
    --color-ring: var(--ring);
    --color-chart-1: var(--chart-1);
    --color-chart-2: var(--chart-2);
    --color-chart-3: var(--chart-3);
    --color-chart-4: var(--chart-4);
    --color-chart-5: var(--chart-5);
    --radius-sm: calc(var(--radius) - 4px);
    --radius-md: calc(var(--radius) - 2px);
    --radius-lg: var(--radius);
    --radius-xl: calc(var(--radius) + 4px);
    --color-sidebar: var(--sidebar);
    --color-sidebar-foreground: var(--sidebar-foreground);
    --color-sidebar-primary: var(--sidebar-primary);
    --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
    --color-sidebar-accent: var(--sidebar-accent);
    --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
    --color-sidebar-border: var(--sidebar-border);
    --color-sidebar-ring: var(--sidebar-ring);
}

@layer base {
    * {
        @apply border-border outline-ring/50;
    }

    body {
        @apply bg-background text-foreground;
    }
}

/* Utility Classes */
.container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
}

/* Form Styles */
input,
textarea,
select {
    font-family: inherit;
    font-size: 100%;
}

button {
    font-family: inherit;
    cursor: pointer;
}

/* Accessibility */
.sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border-width: 0;
}

/* Focus Styles */
*:focus-visible {
    outline: 2px solid var(--ring);
    outline-offset: 2px;
}
======================================================================
/**
 * @file register-form.css
 * @description Styles for the registration form components
 * @author Sherif Talaat
 * @version 2.0.0
 * @date 10-10-2025
 */
@import "../styles/globals.css";
/* === Form Container === */
.register-form__container {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 1rem;
}

.register-form__card {
    background: var(--translucent-white-1);
    backdrop-filter: blur(24px);
    border: 1px solid var(--translucent-white-2);
    border-radius: 1rem;
    padding: 3.5rem 5rem;
    box-shadow: 0 24px 64px var(--translucent-black-1);
    width: 100%;
}

/* === Form Header === */
.register-form__header {
    text-align: center;
    margin-bottom: 2rem;
}

.register-form__title {
    font-size: 2rem;
    font-weight: 700;
    color: var(--primary-white);
    margin-bottom: 0.5rem;
    background: linear-gradient(135deg, var(--primary-white) 0%, var(--light-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: var(--transparent);
    background-clip: text;
}

.register-form__subtitle {
    font-size: 1rem;
    color: var(--accent-pink);
}

/* === Role Selection === */
.register-form__role-selection {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    margin-bottom: 2rem;
}

/* === Form Elements === */
.register-form__form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
}

/* === Password Requirements === */
.register-form__password-requirements {
    background: var(--translucent-white-3);
    border: 1px solid var(--translucent-white-2);
    border-radius: 0.5rem;
    padding: 1rem;
}

.register-form__requirements-title {
    font-size: 0.875rem;
    color: var(--translucent-white-4);
    margin-bottom: 0.75rem;
    font-weight: 500;
}

.register-form__requirements-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
}

.register-form__requirement-item {
    font-size: 0.875rem;
    color: var(--translucent-white-5);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    transition: color 0.2s;
}

.register-form__requirement-item--valid {
    color: var(--accent-pink);
}

.register-form__requirement-icon {
    width: 1rem;
    text-align: center;
}

/* === Error Message === */
.register-form__error-message {
    font-size: 0.875rem;
    color: var(--vivid-pink);
    margin-top: -0.5rem;
}

/* === Employer Fields === */
.register-form__employer-fields {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
}

/* === Terms and Conditions === */
.register-form__terms-container {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
}

.register-form__terms-label {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    font-size: 0.875rem;
    color: var(--translucent-white-4);
    cursor: pointer;
}

.register-form__terms-checkbox {
    margin-top: 0.25rem;
    cursor: pointer;
}

.register-form__terms-link {
    color: var(--accent-pink);
    text-decoration: underline;
    transition: color 0.2s;
}

    .register-form__terms-link:hover {
        color: var(--vivid-pink);
    }

/* === Submit Button === */
.register-form__submit-button {
    width: 100%;
    padding: 0.875rem 1.5rem;
    background: linear-gradient(135deg, var(--submit-pink-1) 0%, var(--submit-pink-2) 100%);
    color: var(--primary-white);
    border: none;
    border-radius: 0.5rem;
    font-size: 1rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: all 0.3s;
    cursor: pointer;
    box-shadow: 0 8px 24px var(--shadow-pink);
}

.register-form__submit-button--loading {
    animation: flying 3s;
}

.register-form__submit-button:hover:not(:disabled) {
    transform: translateY(-2px);
    box-shadow: 0 10px 30px var(--shadow-pink);
}

.register-form__submit-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}

/* === Sign In Link === */
.register-form__sign-in-link {
    text-align: center;
    margin: 0.75rem 0;
    color: var(--primary-white);
    font-size: 0.875rem;
}

.register-form__sign-in-text {
    display: inline;
    color: var(--primary-white);
    margin-right: 0.375rem;
}

.register-form__sign-in-anchor {
    color: var(--accent-pink);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
}

    .register-form__sign-in-anchor:hover {
        color: var(--vivid-pink);
        text-decoration: underline;
    }

/* === Divider === */
.register-form__divider {
    text-align: center;
    margin: 1rem 0;
    position: relative;
    color: var(--primary-white);
    font-size: 0.8125rem;
    font-weight: 500;
}

    .register-form__divider::before,
    .register-form__divider::after {
        content: "";
        position: absolute;
        top: 50%;
        width: 38%;
        height: 1px;
        background: linear-gradient(to right, transparent, var(--accent-pink), transparent);
    }

    .register-form__divider::before {
        left: 0;
    }

    .register-form__divider::after {
        right: 0;
    }

/* === Social Login === */
.register-form__social-login {
    margin-top: 1rem;
}

.register-form__social-login-text {
    text-align: center;
    font-size: 0.875rem;
    color: var(--translucent-white-5);
    margin-bottom: 1rem;
    position: relative;
}

    .register-form__social-login-text::before,
    .register-form__social-login-text::after {
        content: "";
        position: absolute;
        top: 50%;
        width: 40%;
        height: 1px;
        background: var(--translucent-white-2);
    }

    .register-form__social-login-text::before {
        left: 0;
    }

    .register-form__social-login-text::after {
        right: 0;
    }

.register-form__social-buttons {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
}

.register-form__social-button {
    padding: 0.75rem 1rem;
    border: 1px solid var(--translucent-white-1);
    border-radius: 0.5rem;
    background: var(--translucent-white-1);
    color: var(--primary-white);
    font-size: 0.875rem;
    font-weight: 500;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    transition: all 0.3s;
    cursor: pointer;
}

    .register-form__social-button:hover {
        background: var(--translucent-black-1);
        border-color: var(--accent-pink);
        transform: translateY(-0.125rem);
        box-shadow: 0 8px 24px var(--translucent-black-1);
    }

.register-form__social-button-icon {
    font-size: 1.125rem;
}

/* === Terms Text === */
.register-form__terms-text {
    text-align: center;
    font-size: 1rem;
    color: var(--primary-white);
    margin-top: 1.75rem;
    line-height: 1.6;
}

.register-form__terms-link {
    color: var(--accent-pink);
    text-decoration: none;
    transition: color 0.3s ease;
}

    .register-form__terms-link:hover {
        color: var(--accent-pink);
        text-decoration: underline;
    }

/* === Animations === */
@keyframes flying {
    0%, 30% {
        transform: rotate(-2deg);
    }

    15%, 45% {
        transform: rotate(2deg) scale(1.05);
    }

    61% {
        opacity: 1;
    }

    70% {
        transform: rotate(25deg);
    }

    100% {
        opacity: 0;
        transform: translate(700px) rotate(25deg);
    }
}

/* === Responsive Design === */
@media (max-width: 768px) {
    .register-form__card {
        padding: 1.5rem;
        width: 100%;
    }

    .register-form__title {
        font-size: 1.5rem;
    }

    .register-form__role-selection {
        grid-template-columns: 1fr;
    }

    .register-form__social-buttons {
        grid-template-columns: 1fr;
    }

    .register-form__terms-text {
        font-size: 0.8rem;
    }
}
========================================================================
/**
 * @file registration-page.css
 * @description Styles for the registration page layout and components
 * @author Shahd Mohay
 * @version 2.0.0
 * @date 10-10-2025
 */

/*
    last update on 14-10-2025
    edit on Dark Theme support , make sure all colors are defined in variables , remove hardcoded colors and remove duplicate styles and unused styles
    by Sherif Talaat
*/
@import "./globals.css";
/* === Registration Layout === */
.registration-page {
    width: 100%;
    min-height: 100vh;
    display: flex;
    position: relative;
    overflow: hidden;
    background: linear-gradient(135deg, var(--primary-black) 0%, var(--primary-black) 50%, var(--primary-black) 100%);
}

    .registration-page::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: radial-gradient(circle at 15% 30%, var(--radial-pink-1) 0%, transparent 50%), radial-gradient(circle at 85% 70%, var(--radial-pink-2) 0%, transparent 50%), radial-gradient(circle at 50% 50%, var(--radial-pink-3) 0%, transparent 60%);
        pointer-events: none;
        animation: backgroundPulse 15s ease-in-out infinite;
    }

@keyframes backgroundPulse {
    0%, 100% {
        opacity: 1;
    }

    50% {
        opacity: 0.8;
    }
}

/* === Back Button === */
.registration-page__back-button {
    position: absolute;
    top: 2rem;
    left: 2rem;
    width: 3.25rem;
    height: 3.25rem;
    border-radius: 1rem;
    background: var(--translucent-black-1);
    backdrop-filter: blur(12px);
    border: 1px solid var(--translucent-white-2);
    color: var(--primary-white);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 10;
}

    .registration-page__back-button:hover {
        background: var(--translucent-white-1);
        transform: translateX(-0.25rem);
        border-color: var(--translucent-white-4);
        box-shadow: 0 8px 24px var(--translucent-black-1);
    }

/* === Left Section === */
.registration-page__left-section {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 5rem 3.75rem;
    position: relative;
    gap: 3rem;
}

.registration-page__canvas-container {
    width: 31.25rem;
    height: 31.25rem;
    position: relative;
}

/* === Floating Particles === */
.registration-page__floating-particles {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    overflow: hidden;
}

/* === Welcome Content === */
.registration-page__welcome-content {
    text-align: center;
    color: var(--primary-white);
    max-width: 35rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.registration-page__badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.625rem 1.25rem;
    background: var(--translucent-black-1);
    backdrop-filter: blur(12px);
    border: 1px solid var(--translucent-white-2);
    border-radius: 6.25rem;
    font-size: 0.8125rem;
    font-weight: 500;
    color: var(--translucent-white-4);
    margin: 0 auto;
    box-shadow: 0 0.25rem 1rem var(--translucent-black-1);
}

.registration-page__badge-icon {
    color: var(--accent-pink);
}

.registration-page__welcome-title {
    font-size: 4rem;
    font-weight: 800;
    line-height: 1.1;
    letter-spacing: -0.02em;
    text-shadow: 0 0.25rem 1.5rem var(--shadow-pink);
}

.registration-page__gradient-text {
    background: linear-gradient(135deg, var(--select-pink) 0%, var(--accent-pink) 40%, var(--light-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 4s ease infinite;
    background-size: 200% auto;
}

@keyframes gradientShift {
    0%, 100% {
        background-position: 0% center;
    }

    50% {
        background-position: 100% center;
    }
}

.registration-page__welcome-description {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--muted-foreground);
    font-weight: 400;
}

/* === Features List === */
.registration-page__features-list {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 0.5rem;
}

.registration-page__feature-item {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    padding: 0.75rem 1.25rem;
    background: var(--translucent-black-1);
    backdrop-filter: blur(8px);
    border: 1px solid var(--translucent-white-2);
    border-radius: 0.75rem;
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--translucent-white-4);
    transition: all 0.3s ease;
}

    .registration-page__feature-item:hover {
        background: var(--translucent-white-1);
        border-color: var(--translucent-white-4);
        transform: translateY(-0.125rem);
    }

.registration-page__feature-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 0.5rem;
    background: linear-gradient(135deg, var(--radial-pink-1), var(--radial-pink-2));
    color: var(--accent-pink);
}

/* === Right Section === */
.registration-page__right-section {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 3.75rem;
    background: var(--translucent-black-1);
    backdrop-filter: blur(24px);
    border-left: 1px solid var(--translucent-white-1);
}

/* === Role Selector === */
.registration-page__role-selector {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
    margin-bottom: 2rem;
}

.registration-page__role-button {
    background: var(--translucent-white-1);
    border: 2px solid var(--border);
    border-radius: 1rem;
    padding: 1.25rem 1rem;
    color: var(--primary-white);
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
    text-align: center;
    position: relative;
    overflow: hidden;
}

    .registration-page__role-button::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(135deg, var(--radial-pink-1), var(--radial-pink-2));
        opacity: 0;
        transition: opacity 0.3s ease;
    }

    .registration-page__role-button:hover::before {
        opacity: 1;
    }

    .registration-page__role-button:hover {
        background: var(--translucent-white-3);
        border: 1px solid var(--accent-pink);
        transform: translateY(-0.25rem);
        box-shadow: 0 8px 24px var(--translucent-black-1);
    }

.registration-page__role-button--active {
    background: var(--translucent-white-3);
    border-color: var(--accent-pink);
    box-shadow: 0 0 2rem var(--shadow-pink), 0 8px 24px var(--translucent-black-1);
}

    .registration-page__role-button--active::before {
        opacity: 1;
    }

.registration-page__role-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.75rem;
    height: 2.75rem;
    border-radius: 0.75rem;
    background: var(--translucent-white-1);
    transition: all 0.3s ease;
}

.registration-page__role-button--active .registration-page__role-icon {
    background: linear-gradient(135deg, var(--light-pink), var(--accent-pink));
    color: var(--select-pink);
}

.registration-page__role-text {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    position: relative;
    z-index: 1;
}

.registration-page__role-title {
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: -0.01em;
}

.registration-page__role-subtitle {
    font-size: 0.75rem;
    color: var(--accent-pink);
    font-weight: 400;
}

.registration-page__role-check {
    position: absolute;
    top: 0.75rem;
    right: 0.75rem;
    color: var(--primary-white);
    animation: checkIn 0.3s ease;
}

@keyframes checkIn {
    0% {
        transform: scale(0);
        opacity: 0;
    }

    50% {
        transform: scale(1.2);
    }

    100% {
        transform: scale(1);
        opacity: 1;
    }
}

/* === Input Groups === */
.registration-page__input-group {
    position: relative;
    margin-bottom: 1.25rem;
}

.registration-page__input {
    width: 100%;
    padding: 1rem 1.25rem 1rem 3.25rem;
    background: var(--translucent-white-1);
    border: 2px solid var(--border);
    border-radius: 0.875rem;
    color: var(--primary-white);
    font-size: 0.9375rem;
    font-weight: 400;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

    .registration-page__input::placeholder {
        color: var(--primary-white);
    }

    .registration-page__input:focus {
        outline: none;
        background: var(--translucent-white-3);
        border-color: var(--accent-pink);
        box-shadow: 0 0 0 0.25rem var(--shadow-pink);
    }

.registration-page__input-icon {
    position: absolute;
    left: 1.125rem;
    top: 50%;
    transform: translateY(-50%);
    color: var(--primary-white);
    transition: color 0.3s ease;
}

.registration-page__input:focus ~ .registration-page__input-icon {
    color: var(--accent-pink);
}

.registration-page__toggle-password {
    position: absolute;
    right: 1.125rem;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    color: var(--primary-white);
    cursor: pointer;
    padding: 0.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    border-radius: 0.5rem;
}

    .registration-page__toggle-password:hover {
        color: var(--primary-white);
        background: var(--translucent-white-3);
    }

/* === Create Account Button === */
.registration-page__create-account-button {
    width: 100%;
    padding: 1rem;
    background: linear-gradient(135deg, var(--submit-pink-1) 0%, var(--submit-pink-2) 100%);
    border: none;
    border-radius: 0.875rem;
    color: var(--primary-white);
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.625rem;
    margin-top: 0.75rem;
    box-shadow: 0 8px 24px var(--shadow-pink);
    position: relative;
    overflow: hidden;
}

    .registration-page__create-account-button::before {
        content: "";
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, var(--translucent-white-4), transparent);
        transition: left 0.5s ease;
    }

    .registration-page__create-account-button:hover::before {
        left: 100%;
    }

    .registration-page__create-account-button:hover {
        transform: translateY(-0.125rem);
        box-shadow: 0 12px 32px var(--shadow-pink);
        background: linear-gradient(135deg, var(--submit-pink-2) 0%, var(--light-pink) 100%);
    }

    .registration-page__create-account-button:active {
        transform: translateY(0);
    }

/* === Form Card === */
.registration-page__form-card {
    width: 100%;
    max-width: 32.5rem;
    background: var(--translucent-white-1);
    backdrop-filter: blur(24px);
    border-radius: 2rem;
    border: 1px solid var(--translucent-white-2);
    padding: 3rem;
    box-shadow: 0 24px 64px var(--translucent-black-1);
}

.registration-page__form-header {
    text-align: center;
    margin-bottom: 2.5rem;
    color: var(--primary-white);
}

.registration-page__form-title {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    letter-spacing: -0.01em;
    background: linear-gradient(135deg, var(--primary-white) 0%, var(--light-pink) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.registration-page__form-description {
    font-size: 0.9375rem;
    color: var(--accent-pink);
    font-weight: 400;
}

/* === Responsive Design === */
@media (max-width: 75rem) {
    .registration-page__left-section {
        padding: 3.75rem 2.5rem;
    }

    .registration-page__canvas-container {
        width: 25rem;
        height: 25rem;
    }

    .registration-page__welcome-title {
        font-size: 3.25rem;
    }

    .registration-page__right-section {
        padding: 2.5rem;
    }

    .registration-page__form-card {
        padding: 2.5rem;
    }
}

@media (max-width: 60.5rem) {
    .registration-page {
        flex-direction: column;
    }

    .registration-page__left-section,
    .registration-page__right-section {
        flex: none;
        width: 100%;
        max-width: 100%;
    }

    .registration-page__left-section {
        padding: 3.75rem 2rem;
        gap: 2rem;
    }

    .registration-page__canvas-container {
        width: 20rem;
        height: 20rem;
    }

    .registration-page__welcome-title {
        font-size: 2.625rem;
    }

    .registration-page__right-section {
        border-left: none;
        border-top: 1px solid var(--translucent-white-2);
        padding: 2.5rem 2rem;
    }

    .registration-page__form-card {
        max-width: 100%;
        padding: 2rem;
    }

    .registration-page__role-selector {
        grid-template-columns: 1fr;
    }

    .registration-page__role-button {
        flex-direction: row;
        justify-content: flex-start;
        padding: 1rem 1.25rem;
    }

    .registration-page__role-text {
        align-items: flex-start;
        text-align: left;
    }
}

@media (max-width: 40rem) {
    .registration-page__back-button {
        top: 1.25rem;
        left: 1.25rem;
        width: 2.75rem;
        height: 2.75rem;
    }

    .registration-page__left-section {
        padding: 2.5rem 1.5rem;
    }

    .registration-page__canvas-container {
        width: 17.5rem;
        height: 17.5rem;
    }

    .registration-page__welcome-title {
        font-size: 2.25rem;
    }

    .registration-page__welcome-description {
        font-size: 1rem;
    }

    .registration-page__features-list {
        flex-direction: column;
        align-items: stretch;
    }

    .registration-page__right-section {
        padding: 2rem 1.5rem;
    }

    .registration-page__form-card {
        padding: 1.75rem;
        border-radius: 1.5rem;
    }

    .registration-page__form-title {
        font-size: 1.75rem;
    }

    .registration-page__social-login {
        grid-template-columns: 1fr;
    }
}
